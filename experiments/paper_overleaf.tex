\documentclass[journal]{IEEEtran}
\usepackage{graphicx}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{cite}
\usepackage{multirow}
\usepackage{array}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algorithmic}

\begin{document}

\title{An Empirical Study of VQ Routing Behavior in Traffic Forecasting}

\author{Author Name\\Department, University\\Email}

\maketitle

\begin{abstract}
Dynamic graph routing is widely adopted in spatio-temporal traffic forecasting, yet the actual routing behaviors learned after training convergence remain largely unexamined. We conduct an empirical diagnosis of VQ-based discrete graph routing built upon the DGSTA backbone on the PeMS08 dataset. Our analysis reveals that VQ routing converges to depth-wise template specialization: different encoder layers consistently prefer different graph templates, while within each layer, template selection is predominantly static across time periods. Freezing routing to dominant templates in the converged model causes negligible performance degradation ($<$0.03 MAE), suggesting that inference-time routing dynamicity plays a minor role. This creates an apparent paradox: the router's real-time decisions are largely dispensable, yet replacing the VQ mechanism with independently learned per-layer static graphs causes performance to collapse below the original baseline. Training trajectory analysis offers a potential resolution: the router maintains near-maximum entropy ($\sim$0.99) for the vast majority of training, indicating an extended phase of broad structural exploration that static graphs bypass entirely. Geographic visualization of learned templates reveals interpretable spatial patterns---including local propagation and long-range corridor coupling---with quantitative fidelity metrics showing 30--50$\times$ larger degradation under random template substitution. Taken together, these observations suggest that the VQ router's primary contribution may lie in training-time structural exploration rather than inference-time graph switching. DiMoR achieves competitive forecasting performance while providing these interpretable routing behaviors.
\end{abstract}

\begin{IEEEkeywords}
traffic forecasting, dynamic graph routing, explainability, vector quantization, empirical analysis
\end{IEEEkeywords}

\section{Introduction}

Traffic flow prediction is a fundamental task in intelligent transportation systems. Recent advances have increasingly adopted dynamic graph mechanisms---models that learn time-varying adjacency structures to capture evolving spatial dependencies among road sensors. These methods operate under the implicit assumption that traffic spatial dependencies change meaningfully over time and that learning to switch between graph structures improves prediction.

However, relatively few studies have examined \emph{what routing behaviors are actually learned} after training converges. Do graph routers produce time-varying structures in practice? Does real-time routing contribute meaningfully to prediction accuracy? And what spatial patterns, if any, do the learned graph templates encode?

In this work, we conduct an empirical diagnosis of VQ-based graph routing in the context of traffic forecasting, using the DGSTA architecture as our backbone. Rather than proposing a novel architecture, our contribution is a structured analysis of router behavior, spanning layer-wise template usage, frozen replay validation, ablation studies, and geographic interpretation of learned graph templates with quantitative spatial statistics.

Our observations on PeMS08 reveal an apparent paradox. On one hand, VQ routing converges to predominantly static, layer-specific template assignments---freezing these assignments at inference time causes negligible degradation ($<$0.03 MAE). On the other hand, simplifying the router into independently learned per-layer static graphs causes performance to collapse below the original DGSTA baseline. This tension---real-time routing decisions matter little, yet the routing mechanism appears essential during training---motivates the central investigation of this work. Training trajectory analysis suggests a potential resolution: the router undergoes an extended high-entropy exploration phase ($\sim$220 epochs) before converging to specialization, a structural search process that static parameterization cannot replicate.

\section{Related Work}

\subsection{Traffic Flow Prediction}

Deep learning for traffic prediction has evolved from RNN-based models to graph neural networks and Transformer architectures. DGSTA combines dynamic graph convolution with spatio-temporal self-attention, achieving competitive performance on PeMS benchmarks. Subsequent works have proposed adaptive adjacency learning, multi-graph fusion, and trend decomposition.

\subsection{Dynamic Graph Routing}

Vector Quantized (VQ) routing, inspired by VQ-VAE and mixture-of-experts architectures, maintains a discrete codebook of graph templates and uses Gumbel-Softmax to select among them. This approach has been applied to traffic forecasting. However, the actual learned behaviors---whether routers produce dynamic graphs or converge to stable specialization---have not been systematically characterized.

\subsection{Explainability in Traffic Forecasting}

Most explainability efforts focus on attention visualization or case studies. Few works examine the internal mechanisms of dynamic graph modules. Our work provides a structured diagnosis of VQ routing behavior, combining layer-wise analysis with quantitative spatial characterization of learned templates and XAI-aligned fidelity metrics.

\section{Method}

\subsection{Backbone: DGSTA}

DiMoR is built upon the DGSTA backbone. Given historical traffic observations $\mathbf{X} \in \mathbb{R}^{B \times T_{in} \times N \times F}$ with batch size $B$, $T_{in}=12$ input steps, $N$ sensors, and $F$ features, the backbone encodes the input through a data embedding layer that combines value, positional, time-of-day, day-of-week, Laplacian spatial, and temporal prior embeddings.

The embedded representation passes through $L=6$ encoder layers. Each layer contains spatio-temporal self-attention (temporal, geographic, and semantic attention heads), followed by an MLP with residual connections and stochastic depth. Skip connections accumulate across layers and are projected via $1\times1$ convolutions to produce the final prediction $\hat{\mathbf{Y}} \in \mathbb{R}^{B \times T_{out} \times N \times 1}$ with $T_{out}=12$.

\subsection{VQ Graph Router}

The VQ Router operates within each encoder layer independently. Each layer maintains a codebook of $K=10$ learnable graph templates $\{\mathbf{G}_k \in \mathbb{R}^{N \times N}\}_{k=1}^{K}$. Given the layer input $\mathbf{x} \in \mathbb{R}^{B \times T \times N \times D}$, the router proceeds in three stages:

\noindent\textbf{Decomposition and Routing.} A moving-average kernel separates $\mathbf{x}$ into trend $\mathbf{x}_{trend}$ and residual $\mathbf{x}_{res}$. The trend component is encoded through a spatial MLP to produce routing logits $\mathbf{z} \in \mathbb{R}^{B \times T \times K}$, from which a discrete template selection is obtained via Gumbel-Softmax with hard sampling:
\begin{equation}
\mathbf{w}_{b,t} = \text{Gumbel-Softmax}(\mathbf{z}_{b,t}, \tau=1.0, \text{hard=True}) \in \{0,1\}^{K}
\label{eq:gumbel}
\end{equation}

\noindent\textbf{Graph Construction and Propagation.} The selected template forms $\mathbf{A}_{vq} \in \mathbb{R}^{B \times T \times N \times N}$ via $\mathbf{A}_{vq} = \sum_k \mathbf{w}_{b,t}^{(k)} \mathbf{G}_k$, while learned node embeddings provide a complementary static graph $\mathbf{A}_{adp} = \text{ReLU}(\mathbf{U}\mathbf{V}^\top)$. Both are sparsified (top-20 per row, softmax-normalized) and used as support matrices in a 2-hop GCN operating on $\mathbf{x}_{res}$. The GCN output is fused with the trend component:
\begin{equation}
\mathbf{x}_{out} = \text{LayerNorm}\big(\mathbf{x}_{trend} + \tanh(\text{GCN}(\mathbf{x}_{res}; \mathbf{A}_{vq}, \mathbf{A}_{adp})) \times \alpha\big)
\label{eq:fusion}
\end{equation}
with learnable scale $\alpha$ initialized at 0.1.

\noindent\textbf{Consistency Regularization.} A temporal smoothness penalty discourages rapid switching:
\begin{equation}
\mathcal{L}_{cons} = \frac{1}{B(T-1)K} \sum_{b,t,k} \big(\mathbf{z}_{b,t+1}^{(k)} - \mathbf{z}_{b,t}^{(k)}\big)^2
\label{eq:cons}
\end{equation}

\subsection{Auxiliary Modules}

\textbf{DelayConv}: A causal depthwise temporal convolution (kernel size 3, Dirac initialization) applied to GCN inputs for local temporal smoothing. \textbf{DeepTrendNet}: A lightweight MLP predicting future values from the trend component of the input traffic signal, fused via a learnable scalar weight. \textbf{Semantic Attention}: A DTW-based attention mask that allows nodes with similar daily traffic patterns to attend to each other.

All modules are config-gated via JSON configuration. The default configuration (all gates off) produces the unmodified DGSTA baseline.

\subsection{Model Complexity}

DiMoR contains approximately 1.4M parameters (VQ Router: $\sim$150K, backbone: $\sim$1.2M). Inference takes $\sim$7.3s on an NVIDIA RTX 5070 Ti, comparable to the baseline DGSTA ($\sim$7.2s).

\section{Experiments}

\subsection{Setup}

We use the PeMS08 dataset (170 sensors, 5-minute intervals, July--August 2016, California highway network). Input and output windows are both 12 steps (60 minutes), split 60\%/20\%/20\% for train/val/test. StandardScaler normalization. Training: AdamW, cosine LR schedule (initial lr=$10^{-3}$, weight decay=0.05), batch size 32, curriculum learning, 300 epochs, early stopping (patience=50). Single NVIDIA RTX 5070 Ti GPU.

\subsection{Main Results}

While Table~\ref{tab:ablation} establishes that the VQ Router contributes meaningfully to prediction accuracy, it does not reveal \emph{how} this contribution is achieved. We next examine the router's internal behavior.

\begin{table}[h]
\centering
\caption{Ablation study on PeMS08 (seed=1).}
\label{tab:ablation}
\begin{tabular}{lccc}
\toprule
Model & @3 MAE & @6 MAE & @12 MAE \\
\midrule
DGSTA (baseline) & 12.082 & 12.498 & 13.204 \\
DiMoR (Full) & \textbf{11.894} & \textbf{12.406} & 13.228 \\
$-$ VQ Router & 12.176 & 12.645 & 13.493 \\
$-$ DeepTrendNet & 11.912 & 12.421 & \textbf{13.222} \\
$-$ DelayConv & 12.176 & 12.622 & 13.348 \\
\bottomrule
\end{tabular}
\end{table}

Removing the VQ Router causes the largest degradation, concentrated at short horizons. DeepTrendNet's contribution is marginal---consistent with the backbone attention mechanism already capturing trend information. DelayConv provides moderate improvements. Multi-seed analysis (3 seeds, Appendix~A) reveals seed-to-seed variance ($\sigma\approx 0.10$ MAE) comparable to the observed ablation deltas. Routing behavior patterns are consistent across all seeds.

\subsection{Routing Appears Dynamic, but Converges to Static Specialization}

\textbf{Figure~1} shows (a) per-layer template usage distribution and (b) time-conditioned dominant template selection across five periods. Different encoder layers consistently prefer different templates: Layer 0 selects T6, Layer 2 selects T4, and Layers 3--5 all select T0. Only Layer 1 exhibits distributed usage (T2: 38\%, T0: 20\%, T9: 12\%). For 5 of 6 layers, a single template accounts for $>$99\% of all selections. The time-conditioned analysis confirms these preferences are temporally stable---the dominant template remains unchanged across all five periods for all layers except Layer 1. This is a counterintuitive finding: the router, explicitly designed for time-varying graph selection, learns depth-dependent specialization instead.

\subsection{Inference-Time Dynamicity Matters Surprisingly Little}

To test whether real-time routing decisions are functionally important, we lock each layer's routing to its dominant template and re-run inference. \textbf{Figure~2} shows the resulting MAE degradation: $<$0.03 across all horizons, with multi-seed error bars. The degradation at the 60-minute horizon (@12) is merely $+$0.023, and at 15 minutes (@3) it is $+$0.010---consistently within one standard deviation of zero.

This result is surprising given the architectural complexity of the routing mechanism. Yet it aligns with the static template usage observed in Figure~1: if the router rarely switches templates, freezing template assignments should cause minimal disruption. The negligible degradation indicates that inference-time dynamicity is largely dispensable in the converged model.

\subsection{Why Does Static Replacement Fail?}

The findings above raise a natural question: if the router does not meaningfully switch between templates, why not replace it with simpler per-layer static graphs? We test this by substituting the entire VQ routing pipeline with independently learnable static adjacency matrices---one per encoder layer---eliminating all routing machinery while retaining slightly more parameters.

The static per-layer baseline achieves 12.889 / 13.631 / 14.879 (@3/@6/@12)---significantly worse than both the VQ Router (11.894 / 12.406 / 13.228) and the original DGSTA baseline (12.082 / 12.498 / 13.204). This collapse is striking: if VQ routing merely provides layer-specific static priors, a simpler static mechanism should approach its performance. The fact that it does not---indeed, that it underperforms even the routing-free baseline---suggests something nontrivial is contributed by the VQ mechanism.

\textbf{Figure~5} offers a potential explanation. Tracking routing entropy at 15 sampled checkpoints across training reveals an unexpected trajectory: the router maintains near-maximum entropy ($H_{norm} \approx 0.99$) for over 220 epochs before entering a final convergence phase. In other words, for the vast majority of training, the router evaluates all templates nearly equally---a state of broad structural exploration---and only commits to specialized assignments in the final stage. Static graphs, initialized randomly and optimized independently, bypass this exploration phase entirely. This contrast suggests that the prolonged exploration enabled by codebook competition may be the mechanism through which VQ routing achieves what static parameterization cannot. We emphasize that this interpretation remains a plausible hypothesis; rigorous causal isolation of the training dynamics is left for future work.

\subsection{What Spatial Structures Do Templates Actually Encode?}

The analyses above focus on \emph{how} the router behaves---but not \emph{what} it has learned. We now examine the spatial semantics of the learned templates.

\textbf{Geographic Characterization.} \textbf{Figure~3} maps the four actively used templates to the PeMS08 sensor network using a topology-preserving layout. Quantitative hop-distance analysis (Appendix~C) confirms: T0 exhibits 100\% local edges (within 3 hops), consistent with neighborhood propagation; T2 shows 61\% long-range edges (beyond 5 hops), consistent with corridor coupling. The contrast is not subtle---T0 connects exclusively adjacent sensors, while T2 frequently spans the highway network. \textbf{Figure~4} adds a temporal dimension: Layer 1, the only layer showing routing variation, increases its use of T2 (long-range corridor coupling) to 59--76\% during daytime peaks and shifts to T0 (local propagation) to 37--57\% at night. This shift is consistent with the intuitive transition from long-range commuting to localized nocturnal activity. \textbf{Figure~7} provides micro-geographic confirmation at the individual sensor level.

\textbf{Structural Disentanglement.} \textbf{Figure~6} presents the Jaccard similarity index of edge overlaps among the four active templates. The near-zero off-diagonal values ($\leq$0.018, mean 0.009) demonstrate that the router extracts highly disjoint, mutually complementary topological priors---not slight variations of a single base graph. Each template contributes a structurally distinct edge set.

\textbf{Quantitative Fidelity.} Table~\ref{tab:xai} reports three XAI-aligned interpretability metrics. (1) \emph{Fidelity}: substituting active templates with random graphs of identical sparsity causes MAE degradation of $+$0.44/$+$0.65/$+$0.99---approximately 30--50$\times$ larger than frozen replay degradation. This demonstrates that learned templates are not arbitrary; they encode faithful spatial dependencies. (2) \emph{Disentanglement}: the mean off-diagonal Jaccard index of 0.009 confirms mutually exclusive template structures. (3) \emph{Locality}: hop-distance distributions (T0: 100\% $<$3 hops; T2: 61\% $>$5 hops) quantify the receptive-field characteristics.

\begin{table}[h]
\centering
\caption{Quantitative Metrics for VQ Routing Interpretability.}
\label{tab:xai}
\begin{tabular}{lcc}
\toprule
Metric & Measurement & Value \\
\midrule
Fidelity & $\Delta$MAE (Random $-$ Original) / (Frozen $-$ Original) & $+$0.44/+0.65/+0.99 / $+$0.01/+0.01/+0.02 \\
Disentanglement & Mean Off-diagonal Jaccard & 0.009 \\[4pt]
Locality (T0) & Edges within $<$3 road hops & 100.0\% \\
Locality (T2) & Edges beyond $>$5 road hops & 60.9\% \\
\bottomrule
\end{tabular}
\end{table}

\section{Discussion}

Taken together, our experiments reveal an apparent paradox: inference-time routing decisions are largely dispensable (Section~4.3, Figure~2), yet removing the VQ routing mechanism during training causes the static per-layer baseline to underperform even the original DGSTA (Section~4.4).

The training trajectory observed in Figure~5 offers a plausible interpretation: the VQ router undergoes an extended phase of broad structural exploration---maintaining near-maximum entropy for the vast majority of training---before converging to stable layer-wise specialization. This prolonged exploration phase, enabled by codebook competition and Gumbel-Softmax routing, may serve as an implicit structural search mechanism. Static graphs, initialized randomly and optimized independently, bypass this exploration entirely, which may explain their failure to match VQ Router performance. We emphasize that this interpretation remains a plausible hypothesis; rigorous causal isolation of the training dynamics---including whether the entropy collapse occurs abruptly or gradually---is left for future work.

\textbf{Limitations.} (1) \emph{Single dataset}: All observations are on PeMS08. The specific spatial semantics are inherently tied to the PeMS highway topology. Cross-dataset validation is needed to assess whether the overarching phenomenon of layer-wise specialization generalizes. (2) \emph{Single backbone}: Findings may depend on DGSTA's strong attention mechanism. (3) \emph{Seed variance}: $\sigma\approx 0.10$ MAE, comparable to observed gains; performance claims are therefore ``competitive'' rather than ``significantly superior.'' (4) \emph{Geographic interpretation}: Template semantic labels are descriptive characterizations supported by quantitative hop-distance statistics, not causal proofs.

\textbf{Implications.} Our observations raise questions for dynamic graph module design. If inference-time routing dynamicity contributes minimally, simpler mechanisms may suffice for certain applications. However, the failure of the static baseline suggests that the training-time dynamics of codebook competition provide optimization benefits that static parameterization cannot replicate. Understanding this distinction may inform more efficient designs---for instance, retaining codebook-based exploration during training while collapsing to static assignments at inference.

\section{Conclusion}

We present an empirical diagnosis of VQ-based graph routing behavior in traffic forecasting. On PeMS08, we observe that VQ routing converges to depth-wise template specialization with limited temporal dynamicity---inference-time routing decisions are largely dispensable, yet the routing mechanism proves essential during training. Geographic analysis reveals that learned templates encode interpretable spatial patterns with high fidelity. We hope these observations---and the tension they highlight between training-time and inference-time contributions---inform the design and evaluation of dynamic graph modules in spatio-temporal forecasting.

\appendix

\section{Multi-Seed Analysis}

\begin{table}[h]
\centering
\caption{Full model across 3 seeds.}
\begin{tabular}{cccc}
\toprule
Seed & @3 MAE & @6 MAE & @12 MAE \\
\midrule
1 & 11.894 & 12.406 & 13.228 \\
0 & 12.117 & 12.570 & 13.306 \\
2 & 12.056 & 12.565 & 13.354 \\
\midrule
Mean $\pm$ Std & 12.022 $\pm$ 0.10 & 12.514 $\pm$ 0.08 & 13.296 $\pm$ 0.05 \\
\bottomrule
\end{tabular}
\end{table}

\section{Experiment Cache Index}

\begin{table}[h]
\centering
\caption{Experiment catalog for reproducibility.}
\begin{tabular}{lccc}
\toprule
Experiment & Exp ID & Config & Seed \\
\midrule
Full (best) & 71098 & VQ+Trend+Delay & 1 \\
$-$ VQ Router & 68783 & Trend+Delay & 1 \\
$-$ DeepTrendNet & 64832 & VQ+Delay & 1 \\
$-$ DelayConv & 43876 & VQ+Trend & 1 \\
Static Per-Layer & 75685 & Static+Trend+Delay & 1 \\
Full & 61239 & VQ+Trend+Delay & 0 \\
Full & 16450 & VQ+Trend+Delay & 2 \\
\bottomrule
\end{tabular}
\end{table}

\section{Template Hop-Distance Statistics}

\begin{table}[h]
\centering
\caption{Edge hop-distance distribution per template.}
\begin{tabular}{lcccc}
\toprule
Template & Local ($<$3 hop) & Mid (3--5 hop) & Long ($>$5 hop) & Interpretation \\
\midrule
T0 & 100.0\% & 0.0\% & 0.0\% & Local propagation \\
T2 & 8.7\% & 30.4\% & 60.9\% & Long-range corridor \\
T4 & 3.8\% & 46.2\% & 50.0\% & Long-range corridor \\
T6 & 5.6\% & 55.6\% & 38.9\% & Mixed-range \\
\bottomrule
\end{tabular}
\end{table}

\section{Multi-Seed Frozen Replay}

\begin{table}[h]
\centering
\caption{Frozen replay across 3 seeds.}
\begin{tabular}{cccc}
\toprule
Seed & @3 $\Delta$MAE & @6 $\Delta$MAE & @12 $\Delta$MAE \\
\midrule
1 & $+$0.010 & $+$0.013 & $+$0.023 \\
0 & $+$0.017 & $+$0.018 & $+$0.020 \\
2 & $+$0.016 & $+$0.017 & $+$0.019 \\
\midrule
Mean $\pm$ Std & $+$0.014 $\pm$ 0.003 & $+$0.016 $\pm$ 0.002 & $+$0.021 $\pm$ 0.002 \\
\bottomrule
\end{tabular}
\end{table}

\end{document}
